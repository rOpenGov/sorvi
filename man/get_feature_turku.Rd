% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wfs.R
\name{get_feature_turku}
\alias{get_feature_turku}
\title{Download a WFS feature layer from Turku's geoservice}
\usage{
get_feature_turku(layer, crs = "EPSG:3067")
}
\arguments{
\item{layer}{Character. The feature type (layer name), e.g. \code{"GIS:Aanestysalueet"}.
A full list of layers can be obtained with \code{turku_features()}.}

\item{crs}{Character. The target coordinate reference system (default: \code{"EPSG:3067"}).}
}
\value{
An \code{sf} object with the requested geometries and attributes.
}
\description{
Retrieves a feature collection as an \code{sf} object from the City of Turku
WFS server, using the specified layer name and coordinate reference system.
}
\examples{
\dontrun{
  # List layers
  turku_features()

  # Download voting districts in EPSG:3067
  aanestysalueet <- turku_get("GIS:Aanestysalueet", crs = "EPSG:3067")
  plot(sf::st_geometry(aanestysalueet))
}
}
